#include <stdio.h>
int main()
{
    int number;

    printf("input: ");
    scanf("%d", &i);

    if(number % 2 == 0)
        printf("%d output: even.", i);
    else
        printf("%d output: odd.", i);

    return 0;
}
